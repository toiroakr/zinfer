---
"zinfer-mini": patch
---

Support current Zod Mini schema APIs (including function schemas returned by apply) and nested iso/coerce namespaces, preserve recursive JSON types, and retain symbol-key identity.
