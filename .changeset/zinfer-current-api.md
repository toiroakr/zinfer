---
"zinfer": patch
---

Support current Zod schema APIs, preserve recursive JSON types, and retain symbol-key identity in generated declarations, including explicitly annotated schemas with distinct input and output types. Exclude ordinary method calls from schema detection.
