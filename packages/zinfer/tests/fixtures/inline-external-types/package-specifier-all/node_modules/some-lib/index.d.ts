// A real (though local-only) node_modules package, standing in for a
// devDependency: `--inline-type-references=all` resolves `Foo` through
// TypeScript's own module resolution rather than filesystem probing.
export type Bar = { nested: true };
export type Foo = { real: true; bar: Bar };
